<div id="adminBar" class="row expanded">
  <div class="column text-right">
    <div>
      <% if user_signed_in? %>
        <%= current_user.email %> -
        <%= link_to "editar", edit_user_registration_path %> -
        <%= link_to "salir", destroy_user_session_path, :method => :delete  %>
      <% end %>
    </div>
  </div>
</div>
