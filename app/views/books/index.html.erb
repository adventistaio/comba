<p id="notice"><%= notice %></p>
<h1>Libros</h1>

<div class="">
  <h2>Antiguo Testamento</h2>
  <% @books.where('section_id = ?', 1).each do |book| %>
  <div>
    <%= link_to book.name, book_path(book) %>
  </div>
  <% end %>
</div>


<div class="">
  <h2>Nuevo Testamento</h2>
  <% @books.where('section_id = ?', 2).each do |book| %>
  <div>
    <%= link_to book.name, book_path(book) %>
  </div>
  <% end %>
</div>
