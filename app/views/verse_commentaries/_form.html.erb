<%= form_for([@verse, @verse_commentary]) do |f| %>
  <% if verse_commentary.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(verse_commentary.errors.count, "error") %> prohibited this verse_commentary from being saved:</h2>

      <ul>
      <% verse_commentary.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.rich_text_area :comment %>
  </div>

  <div class="actions text-right">
    <%= f.submit class: "button" %>
  </div>
<% end %>
